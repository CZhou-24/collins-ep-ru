# Collins ep v0.3.3: finish official acceptance

Continue the reviewed `/bigTMD/collins_ep_analytic` candidate with the immutable external `/Collins-ep-analytic-validation-v0.3.3` release. Use `/Collins-ep-analytic-state-v0.3.3` for new outputs.

The previous two fresh workflows passed through d15 and scoped scientific source review is documented. Each official seed has 1,277 PASS and three native-process BLOCKED rows. The validator wrote 23-character parameter tokens into a 20-character reader. The one-line external formatting diagnostic passes all 62 native checks per seed. Independent review reran all six compiled drivers, reproducing the failure and repaired amplitude agreement. v0.3.3 incorporates the formatting repair; no physical input or equation changes.

This mission primarily executes and verifies the saved implementation. No new analytical stage is planned.

1. Read the new review/install guide and README, and the previous milestone/source-review records. Run `selftest.py` and `workflow.py doctor`. **Do not rerun the scaffold installer.** Preserve original sources, baseline, numerical code, validators, runtime, historical evidence and the original v0.3.0 installation lock.

2. Keep the existing Python interpreter consistent throughout (Python 3.10 in the supplied campaign). Python 3.12's changed float summation can prevent exact historical request replay. Do not switch interpreters mid-campaign, weaken equality, rewrite requests or apply the retrospective sum adapter to official checks.

3. Confirm production and runtime match the reviewed source freeze. Retain SIDIS conventions at commit `5062dcb2407594dafcc2f9f72800e96ff9e6d957`, explicit TMD/Collins extensions and the unchanged 31-obligation contract. Carry forward the source review with its common bare-virtual construction, physical-J0/regulator order, retarded boundary and homogeneous projection explicitly stated. Preserve the 70-row table, literal references and unresolved statuses without automatic promotion.

4. Follow the README to run two distinct fresh nonresumed workflows through d15, official verification for seeds 1729 and 92741, and the pair comparator. Keep sources/runtime unchanged between runs. Each verifier must generate, compile and execute native MadGraph and evaluate the actual candidate amplitudes. Earlier diagnostics cannot replace those gates; v0.3.2 manifests cannot be relabeled. Put all evidence and reports in new external destinations.

5. Diagnose any concrete failure. Correct routine environment problems or editable candidate defects when supported by evidence. Preserve a necessary protected-validator correction as an unapplied patch and minimal reproducer for independent review. Do not change protected files, coupling values, conventions, references, tolerances, proof requirements or status flags to force acceptance. Avoid expensive repeats that merely rediscover a known blocker. New analytical work is justified only by a specific missing or incorrect in-scope step.

Deliver both authoritative seed reports and their pair, complete native source/build/helicity evidence, source/runtime/validator/run identities, and a concise external milestone report. Cite source-review judgment separately from automated acceptance. Package evidence with typed manifests, checksums and native symlinks preserved as links. Do not edit in-tree reports after the acceptance source freeze.

A full CHECKS_PASS plus scoped source review supports the declared Born/one-loop leading-power small-qT/narrow-jet construction with diagonal homogeneous twist-three matching. It does not certify all 70 reference equations or full polarized fixed-order NLO. Full NLO is next; organization and reverse unitarity remain deferred. Fitting and Figure 6 remain paused. Keep BLAS/OpenMP single-threaded.

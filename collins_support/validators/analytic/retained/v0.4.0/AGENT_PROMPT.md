# Collins ep v0.4.0 implementation mission

Implement the additive one-loop leading-power extension under `/bigTMD/collins_ep_analytic`, using the separately installed immutable `/Collins-ep-analytic-validation-v0.4.0`. This is **new analytical implementation**, not another execution-only repair mission. Start from the accepted v0.3.3 checkpoint and preserve every file in `accepted_sources.json`, the old validators, native installations, baselines and historical evidence.

Read `README.md`, `INSTALL.md`, `contract.json`, `docs/SCOPE.md`, `docs/INTERFACES.md` and `docs/SCIENTIFIC_REVIEW.md` in the new validator. Install its additive interface if it is not already installed; never run its retained scaffold installer. Run the real native transport smoke before expensive derivations. Use the campaign's Python 3.10 environment and single-threaded BLAS/OpenMP.

**Physical target.** Unpolarized e + transversely polarized p -> e + jet(h) + X and its Collins modulation, following 2007.07281v1, Sec. II.B and Appendix A. Keep anti-kt and the standard jet axis. The eventual benchmark remains R=1, qT/pT_jet<0.3 and jT<1.5 GeV with the paper's other settings. Work within the leading-power framework. Preserve and investigate the existing narrow-cone versus R~O(1) qualification; coefficient agreement does not itself establish finite-R validity. No unrestricted fixed-order recoil or general power-correction calculation is requested.

**Conventions.** Keep applicable SIDIS conventions at commit `5062dcb2407594dafcc2f9f72800e96ff9e6d957`, the accepted native spin/rapidity extensions, and explicit transformations to references. The transport coupling is a=alpha_s/(2*pi), with the existing SIDIS-to-transport map retained. Keep alpha_EM^2 for photon exchange and retain the literal paper-prefactor discrepancy separately. Derive signs, masses, z factors, Fourier measures, operator normalization and scheme shifts. Never insert an empirical 2z factor or rename a paper equation to force agreement.

**Implement these candidate-owned stages and their libraries:**

1. `common/d16_nlo.wls`: define the measured observable, Hhat/HF operator basis, Wilson-line/gauge completion, EOM and endpoint conventions. Derive the native-to-reference map, state the D-dimensional spin prescription, and identify the paper's angular/operator conventions explicitly. Preserve alternative literal paper interpretations when they are not established.
2. `common/d17_nlo.wls`: derive the independent two-fraction HF contribution to the Collins small-b OPE and its mixing into Hhat. Keep regulated integrands, projections, reductions, masters, counterterms and finite exports. Derive both bulk and endpoint/contact terms. Obtain an independent native route, e.g. a transverse-momentum tail/Fourier calculation with the required D-dimensional finite information. A four-dimensional tail or a pole kernel alone cannot determine all finite terms. This mission does not claim a closed evolution system for every twist-three operator. Preserve the existing homogeneous result as an explicit limit.
3. `common/d18_nlo.wls`: derive the complete finite quark fragmenting-jet/TMD relation at the declared accuracy, separately checking UU and Collins projections. Use the ep measurement and prove the overlap subtraction. Eq. (18) of 2007.07281 is stated through NLL; do not infer missing finite terms from that statement. Do not multiply an extra inclusive jet factor or transplant the inclusive-pp out-of-jet hard matching coefficient. Document the radius validity question. Use an independent native calculation for the finite remainder.
4. `common/d19_nlo.wls`: consume the actual new coefficients in native symbolic UU/UT expressions, with HF independent. Export the strict first-order expansion, common integration functional and resulting asymmetry expansion. Separate the all-b operator formula from its local small-b OPE. Recover the retained native approximation when HF and the additional finite-jet remainder are switched off. Keep that paper-limit comparison distinct from the extended result.

Use FeynArts/FeynCalc and the existing Kira/SubTropica infrastructure where the integral topology requires them. Regenerate from the operator/amplitude inputs; previous final coefficients and literature formulas are comparison targets, not a derivation source. Reusing accepted **derivation code** and shared masters is appropriate. Do not copy final expressions into a new stage and describe that as fresh derivation. The current radial route is not reverse unitarity.

**References.** Retain 2007.07281v1, 1505.05589v1, 1303.2129v1 and 1707.00913v2. Add 1012.3419 (Hhat/HF evolution and EOM), 0903.4680 (Collins transverse tail), and 1705.08443 (fragmenting-jet matching). `references.json` pins the inspected source bytes and records a literal discrepancy between the first two additional references. Compare both kernels, derive the operator conversion, and explain the remaining residual without changing the source equations. Matching one frozen literal alternative alone does not certify that it is physically correct. If a justified native result matches neither, preserve it and submit an exact proposed reference-check correction with the derivation; do not tune it to the harness.

**Validation and development.** Follow the exact packet/provenance interfaces. Implement the upstream epsilon-projector probe at the actual regulated numerator, before the normalized transverse master; propagate it through the derivation and d19. Do not implement it as a direct finite-output adjustment. The HF probe includes its explicitly prescribed endpoint contacts. Keep independent-route inputs separate from primary finite exports. Diagnose concrete failures and fix editable candidate code; do not change protected validators, expected expressions, tolerances or report statuses.

Preserve the 70 historical comparison rows. Add a new comparison with columns for quantity, native expression, literal reference equation and identifier, explicit conversion, derivation origin, agreement, and scope/remaining qualifications. Cross-reference all 26 new and 31 retained obligations. Save `EQUATION_COMPARISON_v0_4_0.md`, `.tex` and a compiled/visually inspected `.pdf` in the production `reports/` directory **before** final source freeze. Extra review notes may be added with new names; old reports remain historical and unchanged.

Then run two fresh workflows through d19, both official NLO seeds, and the official NLO pair as specified in `INSTALL.md`. These commands retain the existing native MadGraph checks, including coherent transverse-spin interference. Finish with a candid scientific source review using `docs/SCIENTIFIC_REVIEW.md`; do not call self-review independent verification. A software CHECKS_PASS does not erase a missing derivation or unresolved physical assumption.

**Deliverables.** Save the final milestone report, exact blockers if any, native source/intermediate/export evidence, reference/convention ledger, two reports and pair, source/runtime/validator hashes and reproduction commands under `/Collins-ep-analytic-state-v0.4.0`. Package a manifest-checked review bundle preserving the existing native symlinks. State separately: retained acceptance, new automated checks, scientific source-review status, the radius qualification, and the remaining full-fixed-order limitations. Do not edit production after its final reports have frozen the source map.

PDFs, transversity and fragmentation functions remain symbolic matrix elements. Fitted providers are unnecessary for acceptance. **Fitting, Figure 6, broad reorganization and reverse unitarity remain deferred.**

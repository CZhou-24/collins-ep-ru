# v0.4.0 — additive analytical implementation contract

- Preserves the exact accepted v0.3.3 validator, including its working MadGraph parser, parameter-card writer, spin controls and generated-symlink handling.
- Freezes the 1,399 accepted candidate files and adds an overwrite-refusing installation path for two coefficient-free interface/configuration files. No native physics stub is installed into the protected validator.
- Adds executable orchestration for candidate-owned d16–d19, native Wolfram reexports, complete coefficient/distribution maps, independent-route evidence and two seeded upstream probes.
- Preserves both literal Collins mixing reference alternatives and all reported discrepancies. Fragmentation and recoil Fourier logarithms remain independent. Both endpoint contact components are transported explicitly.
- Adds fresh two-run verification and pair replay around the unchanged retained checks. Scope and source-review qualifications cannot be promoted by editing report metadata.
- Supplies the implementation prompt, installation commands, packet/provenance specification, obligation coverage table and scientific-review requirements.

This is the machinery for new analytical implementation. It is not a completed d16–d19 calculation or a release of new physical coefficient values. Figure 6, fitted providers, broad reorganization and reverse unitarity remain deferred.
